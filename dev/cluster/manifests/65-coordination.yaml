# A Lease (B90): leader-election holder, keyed to the yggdrasil-db StatefulSet
# so the HOLDER column reads as a real holder.

apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: yggdrasil-db
  namespace: prod
spec:
  holderIdentity: yggdrasil-db-0_6d7f4c1a
  leaseDurationSeconds: 15
  renewTime: "2026-08-15T07:00:00.000000Z"
